import 'package:flutter/material.dart';

class StandingsRoute extends StatelessWidget {
  const StandingsRoute({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
